'use client';
import FileManager from "../../components/file-manager/page"

export default function FileManager_page() {
    return (
        <div className="font-[family-name:var(--font-geist-sans)]">
            <FileManager />

        </div>
    );
}